import React, { FunctionComponent } from 'react';
import { hot } from 'react-hot-loader';

const App: FunctionComponent = (): React.ReactElement => <div>Initial App</div>;

export default hot(module)(App);
